# Uploaded identity documents must never be served over HTTP.
# Anyone who guesses a filename would otherwise read somebody's Aadhaar scan.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
php_flag engine off
